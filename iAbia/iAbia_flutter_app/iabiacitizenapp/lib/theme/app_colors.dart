import 'package:flutter/material.dart';

class AppColors {
  // Light mode colors
  static const primary = Color.fromRGBO(170, 31, 46, 1);
  static const background = Color(0xFFFFFFFF);
  static const text = Color(0xFF000000);

  // Dark mode colors
  static const darkPrimary = Color.fromRGBO(253, 189, 61, 1);
  static const darkBackground = Color(0xFF121212);
  static const darkText = Color.fromARGB(255, 238, 238, 238);
}
